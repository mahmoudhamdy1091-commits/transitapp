# خطة شاملة — مشاكل التعديل والحذف والإلغاء والموافقات

> ملف مرجعي واحد لكل ما يخص هذه العمليات في النظام. الهدف: تجميع كل ما حدث وما يُتوقَّع حدوثه في مكان واحد، مع قواعد عامة تمنع تكرار نفس فئة الخلل في ميزات مستقبلية.
>
> آخر تحديث: 2026-07-01

---

## 1. خلاصة سريعة — السبب الجذري المشترك لكل ما حدث اليوم

كل حادثة في هذا الملف ترجع لواحد (أو أكثر) من 4 أسباب جذرية متكررة:

| # | السبب الجذري | أمثلة |
|---|---|---|
| A | **فحص-ثم-تنفيذ بلا قفل** (Check-then-act / Race condition) | تكرار قيد البيع (ب-1)، تعارض نقرتين متزامنتين على بطاقتين مختلفتين |
| B | **مطابقة "بالموضع/الاتجاه" بدل "بالقيمة الحقيقية"** | فساد COGS في updateJEInPlace (ج-1)، الإفراط في عكس فاتورة معدَّلة (ج-2) |
| C | **حذف/تعديل سجل دون التعامل مع القيد المحاسبي المرتبط** | `deleteSaleInvoice` لا يحذف القيد، `voidSaleInvoice` كان يجمع سيارات محذوفة |
| D | **عدم وضوح بصري/وظيفي بين "طلب جديد" و"تصحيحي الخاص ينتظر تفعيلي"** | رفض تعديل تحصيل بالخطأ بدل الموافقة (حادثة 2026-06-29) |

أي ميزة جديدة تخص تعديل/حذف/إلغاء/موافقة — **اختبرها مقابل هذه الأربعة أولاً** قبل أي شيء آخر.

---

## 2. المشاكل الحقيقية التي حدثت (موثّقة بالـcommit)

| # | المشكلة | الملف | الإصلاح | Commit |
|---|---|---|---|---|
| 1 | تكرار قيد البيع عند موافقات فردية متسارعة على سيارات نفس الفاتورة | operations.js | قفل Promise مشترك `_ensureSaleJE` بمفتاح الفاتورة | `826dff1` |
| 2 | `updateJEInPlace` يكتب مبلغ الإيراد فوق سطر COGS في قيود متعددة المبالغ | engine.js | مطابقة كل سطر بقيمته الفعلية + إعادة حساب COGS الحقيقي | `163d82e` |
| 3 | استثناء `cancelled`/`voided` ناقص في عرض الفاتورة وحساب التعديل | dashboard.js, operations.js | فلتر `not.in.(cancelled,voided)` في 3 مواضع | `2d5b3c7` |
| 4 | `voidSaleInvoice` يُفرِط في عكس فاتورة عُدِّلت سابقاً (يجمع سيارات محذوفة) | dashboard.js | نفس الفلتر أعلاه عند حساب مبلغ العكس | `874ca37` |
| 5 | لوحة التحكم لا تخصم الجانب العكسي لقيود الإلغاء من إجمالي المبيعات | core.js | صافي `cr-dr` بدل جمع `cr` فقط | `ccca81a` |
| 6 | فاتورة بيع جديدة بعدة سيارات = موافقة مطلوبة لكل سيارة | operations.js | تجميع `sales(draft)` بـ`inv_no`، ترحيل الفاتورة كاملة بضغطة واحدة | `33ebaa9` |
| 7 | لا توجد آلية آمنة لعكس قيد يدوي مُرحَّل بدون حذفه | context-menus.js, engine.js | زر "🔄 عكس" + `reverseManualJE` | `73aee3b`, وقبله |
| 8 | حذف قيد يدوي مُرحَّل نهائياً يُفقد الأثر المحاسبي | operations.js | منع الحذف لو `isManual && isPosted`، توجيه لـ"عكس" | `ddd7a5c` |
| 9 | ميزان المراجعة/دفتر الأستاذ بلا رصيد افتتاحي حقيقي، ويُخفي حسابات بلا نشاط بالفترة | accounting.js | `_computeOpeningBalances` + إظهار كل الحسابات | `4b60963`, `4504f01` |

### مرحلة التوحيد (تطبيق مبدأ "وحِّد أولاً ثم انقل" — راجع §4)

| # | التوحيد | كان قبله | الإصلاح الجانبي الناتج تلقائياً | Commit |
|---|---|---|---|---|
| U1 | اعتماد تحصيلات مرتبطة بفاتورة بيع تلقائياً (OPTION B) | نسختان في approveItem و_ensureApprovalJE — إحداهما بلا فحص ref_id، الأخرى بلا audit_log | إغلاق فجوة #11 (audit_log) بلا ترقيع منفصل | `e87e95f` |
| U2 | إنشاء قيد الموافقة لشراء/دفعة/مصروف/صرف شريك | 5+5 نسخة منفصلة، حتى القيمة الافتراضية لطريقة الدفع تفرَّقت ('نقد' مقابل 'تحويل بنكي') | توحيد الافتراضي + فحص ref_id من كل المسارات | `c5d4a6f` |

### مشاكل حقيقية **مكتشَفة وغير مُصلَحة بعد** (مرشَّحة لهذه الخطة):

| # | المشكلة | الموضع | الخطورة | الانتشار الفعلي |
|---|---|---|---|---|
| 10 | `deleteSaleInvoice` لا يعكس القيد المحاسبي قبل الحذف | dashboard.js:1232 | عالية لو استُخدمت على فاتورة مُرحَّلة | **✅ أُصلحت — U3**: الدالة الآن تجلب سجل البيع أولاً؛ لو `posted` تعكس قيد الإيراد+COGS عبر `postDoubleEntry`، وتعكس كل تحصيل مدفوع (`paid_date && posted`) عبر `voidTransaction('collection', c, true)` قبل الحذف الفعلي. 5 دوال حذف أخرى فُحصت وكانت صحيحة بالفعل. |
| 11 | ~~الموافقة التلقائية على تحصيل مرتبط بفاتورة بيع (آلية "OPTION B") لا تُسجِّل audit_log~~ | operations.js | متوسطة | **✅ أُصلحت كنتيجة توحيد U1** — `e87e95f` |
| 12 | رسالة "رفض تعديل تعديل تحصيل" مكرَّرة لفظياً — تُسبِّب التباساً | operations.js (`rejectItem`، بناء الرسالة لـ`*_edit`) | منخفضة (نصية) لكنها ساهمت في حادثة حقيقية | اكتُشفت 2026-06-29 |
| 13 | بطاقة "تعديل معلَّق" في قائمة الموافقات لا تُميِّز بصرياً أنها "تصحيحك الخاص" | operations.js (`renderApprovalList`) | عالية — سبَّبت رفض تصحيح صحيح فعلياً (حادثة BOX-132) | حدثت فعلياً 2026-06-29 |
| 14 | `approvalState.all` حالة مشتركة قابلة للاستبدال الكامل أثناء معالجة خلفية لعنصر آخر | operations.js | منخفضة (تفشل بأمان، لا تُفسد بيانات) لكنها تُربك المستخدم | حدثت فعلياً (خطأ "لم يُعثر على طلب التعديل") |
| 15 | قائمة ⋮ بالموافقات (تعديل/إلغاء سريع/رفض) وعداد الجرس لا يزالان يعاملان كل سيارة كسطر مستقل | operations.js | منخفضة (لا ضرر مالي، فقط نقرات إضافية نادرة) | لم تُختبر فعلياً بعد |

---

## 3. مشاكل متوقَّعة (لم تحدث بعد — استنتاج من الأنماط المتكررة)

| # | السيناريو المتوقَّع | التشابه مع خلل حقيقي | درجة الثقة |
|---|---|---|---|
| 16 | تعديلان متتاليان سريعان على نفس السجل (نفس نمط race الموافقة) قبل اكتمال الأول | مطابق لسبب ب-1 لكن على `*_edit` بدل الموافقة الأولى | متوسطة |
| 17 | "موافقة على الكل" على فاتورة لها تعديل معلَّق + إلغاء تحصيل مرتبط بنفس اللحظة — ترتيب المعالجة غير محدَّد فقد يُنفِّذ الإلغاء قبل التعديل | approveAll يمر بالعناصر بترتيب `approvalState.filtered` كما هو، لا أولوية منطقية بين الأنواع | متوسطة |
| 18 | لا توجد سلة محذوفات (soft-delete) في أي مكان — كل حذف نهائي فوراً | نفس آلية الحذف المباشر المستخدمة في كل الجلسة (`apiDelete`) | عالية (هذه حقيقة بنيوية مؤكَّدة، لا تخمين) |
| 19 | ~~حذف/تعديل دفعة مورد أو مصروف مرتبط بشريك (2400) قد يعاني نفس فجوة "لا حذف للقيد"~~ | تم فحص كل دوال الحذف في U3 | **✅ لا مشكلة**: `deletePaymentEntry`/`deleteExpenseEntry` يستدعيان دائماً `voidTransaction` (لا حذف أبداً)؛ `deleteCollectionEntry` يفحص `paid_date`؛ `deletePayoutEntry` يفحص `post_status`؛ `deleteOpex` يعكس قيده يدوياً ثم يحذف (الجدول بلا `post_status`). |
| 20 | ~~لو أُضيف "حذف ملف/سند شراء كامل" مستقبلاً (غير موجود حالياً)~~ | **تصحيح:** الميزة **موجودة فعلاً** (`confirmDeleteDealFromModal`, reports.js:750) — فاتها الفحص الأول. كانت تحذف audit_log بفلتر file_no **بعد** تسجيل حذفها، فتمحو أثر حذفها هو نفسه وأي تاريخ سابق لنفس الملف. **✅ أُصلحت** | `e267221` |

---

## 4. الإطار العام — قواعد تصميم لمنع هذه الفئة بنيوياً

1. **لا حذف فعلي لسجل `posted` له قيد محاسبي.** أي دالة حذف جديدة تتحقق أولاً من `post_status`؛ لو مُرحَّل، تُمنع وتُوجِّه لإلغاء/عكس. (مُطبَّق على القيد اليدوي فقط حتى الآن — يحتاج تعميماً).
2. **القفل بمفتاح المجموعة لا بمفتاح السطر.** أي عملية تُنشئ قيداً واحداً يمثّل مجموعة سطور تستخدم نفس نمط `_ensureSaleJE` (Promise مشترك بمفتاح المجموعة).
3. **مطابقة بالقيمة الحقيقية لا بالموضع/الاتجاه.** أي تحديث قيد لمجموعة سطور متعددة المبالغ يطابق كل سطر بقيمته الفعلية (نفس مبدأ `updateJEInPlace` بعد الإصلاح).
4. **كل تغيير حالة يُسجَّل في audit_log بلا استثناء — حتى الموافقات التلقائية الجانبية.**
5. **بطاقة الموافقة المعلَّقة تُفرِّق بصرياً بين "طلب جديد" و"تصحيحي الخاص ينتظر تفعيلي".** أي زر رفض على تعديل المستخدم نفسه يحمل نص تحذير صريح: "سيُرجعك للقيمة القديمة الخاطئة".
6. **لا قراءة من حالة مشتركة قابلة للاستبدال أثناء معالجة خلفية لعنصر آخر.** (`approvalState.all` يحتاج لقطة ثابتة أو إعادة جلب آمنة لكل معالجة).
7. **نسخة احتياطية قبل أي حذف يمسح أكثر من سجل واحد** (يدوياً الآن كما حدث في BOX-131 — مرشَّح للتشغيل التلقائي لاحقاً).

---

## 5. خطة العمل المقترحة (بالترتيب، بند واحد كل مرة كالعادة)

| البند | الوصف | يلمس بيانات حقيقية؟ | الحالة |
|---|---|---|---|
| U1 | توحيد اعتماد التحصيلات المرتبطة بفاتورة (OPTION B) | لا | ✅ تم — `e87e95f` |
| U2 | توحيد إنشاء قيد الموافقة لشراء/دفعة/مصروف/صرف شريك | لا | ✅ تم — `c5d4a6f` |
| عاجل | حذف صفقة كاملة (`confirmDeleteDealFromModal`) كان يمحو سجل تدقيقه نفسه | بيانات اختبار معزولة فقط | ✅ تم — `e267221` |
| U3 | فحص كل دوال الحذف (6 دوال) + إصلاح `deleteSaleInvoice`: تعكس قيد البيع+COGS والتحصيلات المدفوعة قبل الحذف الفعلي | لا (تعديل كود فقط) | ✅ تم — `(pending commit)` |
| د-1 | تمييز بصري لبطاقة "تعديل معلَّق" + تحذير صريح عند الرفض (يمنع تكرار حادثة BOX-132) | لا — تعديل عرض فقط | ⏳ |
| د-2 | تصحيح رسالة "رفض تعديل تعديل ..." المكرَّرة | لا | ⏳ |
| د-5 | ~~فحص دوال حذف payments/expenses الأخرى لنفس فجوة C~~ | — | ✅ مُغلَق ضمن U3 — كلها صحيحة |
| د-6 | (اختياري، مؤجَّل) لقطة ثابتة لـ`approvalState.all` عند بدء كل معالجة خلفية | لا | مؤجَّل |
| د-7 | (اختياري، مؤجَّل) إعادة هيكلة: نقل كل دوال التعديل/الحذف/الإلغاء/الموافقة (بعد توحيدها) لملف واحد `js/approvals-lifecycle.js` | لا، لكنه نقل كبير — خطر إعادة الهيكلة نفسه، لا تغيير وظيفي | مؤجَّل لما بعد U1-U3+د-1/د-2 |

**ملاحظة عن د-7 (الملف الموحَّد):** هذا نقل ضخم (~30 دالة من operations.js + engine.js + dashboard.js) بلا تغيير وظيفي — أعلى خطورة من أي بند وظيفي أعلاه لأنه عرضة لكسر شيء بالخطأ في النقل نفسه، وأقل قيمة فورية. أقترح تنفيذه **بعد** إغلاق كل بنود د-1 إلى د-5 الوظيفية، دالة بدالة، باختبار فردي لكل نقلة — لا نقلة جماعية واحدة.

---

## 6. قائمة تحقق (Checklist) — استخدمها عند أي ميزة جديدة تخص تعديل/حذف/إلغاء/موافقة

- [ ] هل العملية الجديدة قد تُنفَّذ مرتين متزامنتين (نقرتان سريعتان)؟ → استخدم قفل بمفتاح المجموعة.
- [ ] هل القيد المرتبط متعدد المبالغ (أكثر من مبلغ واحد بالقيد)؟ → طابق كل سطر بقيمته، لا بموضعه.
- [ ] هل الحذف يمسح سجلاً له قيد `posted`؟ → امنعه، وجِّه لإلغاء/عكس.
- [ ] هل تستثني الحالات `cancelled`/`voided` من أي إجمالي أو حساب مبلغ عكس؟
- [ ] هل كل تغيير حالة (موافقة/رفض/تعديل/حذف) يُسجَّل في audit_log — حتى التلقائي الجانبي؟
- [ ] هل بطاقة الموافقة (لو وُجدت) تُوضِّح بصرياً ماذا يحدث لو ضُغط "رفض" بالتحديد؟
- [ ] هل تعتمد على `approvalState.all` (أو أي حالة مشتركة) أن تبقى ثابتة طوال معالجة خلفية؟ تحقَّق من عدم استبدالها أثناء ذلك.
