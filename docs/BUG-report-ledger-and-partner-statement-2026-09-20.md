# تقرير أعطال — شاشات دفتر الأستاذ / ميزان المراجعة / تقرير الشركاء

**تاريخ الاكتشاف:** 2026-09-20، أثناء محاولة سحب كشف حساب مازن الخلف (2401/TM) من الشاشات الحية للتحقق من تسوية م٤ في `docs/PLAN-partner-accounts-2026-09-17.md`.

**حالة الإصلاح (محدَّثة 2026-09-20):**
- ✅ **البند ١ (دفتر الأستاذ العام) — مُصلَح ومنشور** (commit `82ab4c8`، Wrangler version `2026-09-20T11:50:22Z`). التفاصيل في نهاية هذا الملف.
- ✅ **`_computeOpeningBalances`** (المُستخدَمة في دفتر الأستاذ العام **وميزان المراجعة**) — **مُصلَحة ومنشورة كجزء من نفس الـcommit**.
- ✅ **البند ٢ (ميزان المراجعة — `loadTrialBalance`) — مُصلَح ومنشور** (commit `855c237`، Wrangler version `2026-09-20T12:05:55Z`). **تحقُّق حي:** TM "الكل" رجع 4,510,834.41/4,510,834.41 متوازن (44 حساب) بعد ما كان مقطوعًا؛ حساب 2401 لوحده طلع 123,393.00/76,770.99/فرق 46,622.01 مطابق تمامًا للـSQL. BOX فُحصت ككناري: "الكل" 19,717,552.23/19,717,552.23 متوازن (39 حساب)، وحساب 2401(BOX) طابق الأساس المسجَّل (13,240.00 مدين).
- ✅ **البند ٣ (تقرير الشركاء ← كشف شامل لشريك) — مُصلَح ومنشور** (commit `bf805ce`، Wrangler version `2026-09-20T12:13:53Z`). التفاصيل في نهاية هذا الملف.

كل الأعطال دي مستقلة تمامًا عن صحة تسوية مازن نفسها (اللي جاءت من SQL مباشر في Supabase SQL Editor، غير متأثر بأي حاجة هنا). المشترك بينها كلها: قراءة/عرض بيانات موجودة وصحيحة في القاعدة، لكن بطريقة معطوبة على مستوى الواجهة.

---

## ١. دفتر الأستاذ العام (الشجرة الهرمية) — قطع بيانات صامت بلا أي تحذير — ✅ مُصلَح

**أين:** `loadNewLedger()` — `js/accounting.js:2659-2743`. تُفتح من القائمة الرئيسية ← الحسابات ← دفتر الأستاذ.

**العطل:** الدالة بتجيب **كل** قيود `journal_entries` للنظام كله (كل الحسابات مع بعض) بطلب واحد فقط:
```
&select=...&limit=49999&order=entry_date.asc,id.asc
Range: 0-49999
```
بلا أي حلقة تكرار (pagination loop). Supabase/PostgREST بترجّع حد أقصى ثابت من الصفوف بغض النظر عن حجم الـRange المطلوب — سلوك مُثبَت ومُوثَّق بالفعل في تعليق داخل الكود نفسه (`js/core.js:455-459`) من إصلاح سابق لنفس المشكلة في مكان تاني من التطبيق.

**الدليل الحي (2026-09-20):**
- حساب 2401 (جاري الشريك مازن الخلف) له **151 حركة حقيقية** فعليًا — مؤكَّد من SQL مباشر ومطابق من شاشة دفتر الأستاذ الفردي للحساب (بند ٤).
- شاشة "دفتر الأستاذ العام" تحت فلتر **"الكل" (كل الفترات)** عرضت **62 حركة بس**، متوقفة فجأة عند تاريخ 2024-10-05، بلا أي رسالة أو مؤشر إن البيانات ناقصة.
- ضيّقت الفترة لسنتين كاملتين (2022-01-01 → 2025-12-31) — **نفس الـ62 حركة بالظبط برضه**. يعني القطع مش خاص بـ"كل الفترات" تحديدًا، لكن بأي فترة يكون فيها إجمالي حركة TM (كل الحسابات مجمَّعة، مش حساب مازن لوحده) فوق حد Supabase الفعلي.
- لما قطّعت الفترة سنة سنة (2022-2023، 2024، 2025، 2026 كل واحدة لوحدها)، مجموع الأرقام طلع **لسه ناقص 21 حركة** عن الحقيقة (109,737/60,058.50 بدل 123,393/76,770.99) — يعني حتى بعض السنين المفردة (على الأغلب 2024، اللي فيه أعلى نشاط) لسه بتقطع.

**الخطر:** أي حساب — شريك، بنك، عميل، مصروف — في أي نظام (BOX أو TM) ممكن يظهر على الشاشة دي برصيد ناقص بصمت، من زمان مش بس النهاردة، بلا أي علامة تنبّه المستخدم.

**✅ الإصلاح (2026-09-20، commit `82ab4c8`):** استُخرجت الحلقة الصحيحة (كانت `fetchOne` محلية داخل `fetchJEForPeriod`) لدالة مُصدَّرة `fetchAllPages()` في `js/core.js`، واستُبدل بها الطلب الواحد في `loadNewLedger`. **تحقُّق حي بعد النشر:** حساب 2401(TM) رجع بالظبط لـ151 حركة / 123,393.00 مدين / 76,770.99 دائن — مطابق تمامًا لنتيجة SQL المباشر (كان قبل الإصلاح 62 حركة / 69,086 / 40,283). فُحص BOX كمان (كناري: تحميل الشاشة تحت "الكل" بلا أخطاء، أرقام أكبر ومنطقية بعد الإصلاح). `node --check` نظيف، CRLF محفوظة، الملفات المنشورة مطابقة بايت-لبايت (`cmp`).

---

## ٢. ميزان المراجعة — نفس نمط العطل بالضبط في الكود — ✅ مُصلَح

**أين:** `loadTrialBalance()` — `js/accounting.js:64-96`. أما `_computeOpeningBalances()` — `js/accounting.js:169-189` (تُستخدَم من ميزان المراجعة **ومن** دفتر الأستاذ العام لحساب "الرصيد الافتتاحي") كانت اتصلحت بالفعل كجزء من إصلاح البند ١ (نفس commit `82ab4c8`).

**العطل (قبل الإصلاح):** نفس النمط المعيب حرفيًا — طلب واحد بـ`&limit=49999` + `Range` واحد، بلا حلقة تكرار.

**✅ الإصلاح (2026-09-20، commit `855c237`):** استُبدل الطلب الواحد بنفس `fetchAllPages()` المُصدَّرة من `js/core.js` (المُستخدَمة في إصلاح البند ١). **تحقُّق حي بعد النشر:**
- TM تحت "الكل": 4,510,834.41 مدين / 4,510,834.41 دائن، **متوازن**، 44 حساب (كان مقطوعًا قبل الإصلاح لنفس سبب البند ١).
- حساب 2401(TM) لوحده: 123,393.00 مدين / 76,770.99 دائن / فرق 46,622.01 — **مطابق تمامًا لنتيجة SQL المباشر**.
- BOX (كناري): تحت "الكل" 19,717,552.23/19,717,552.23 متوازن، 39 حساب؛ وحساب 2401(BOX، عبد الرحيم الجاحد) طابق الأساس المسجَّل مسبقًا (13,240.00 مدين) — لا انحراف.
- `node --check` نظيف، CRLF محفوظة، الملفات المنشورة مطابقة بايت-لبايت (`cmp`).

**لم أختبرها بالأرقام الفعلية النهارده** (لم يتّسع الوقت)، لكن الكود **مطابق حرفيًا** للنمط المُثبَت عطبه في البند ١. الاحتمال العملي إنها معطوبة بنفس الطريقة لأي نظام/فترة يتجاوز فيها إجمالي القيود حد Supabase عالٍ جدًا.

**ليه ده أخطر من البند ١:** ميزان المراجعة هو المرجع اللي بيتستخدم طول الوقت (في الجلسة دي وقبلها) كدليل "كل حاجة متوازنة، فرق صفر". لو بيقطع بصمت، الميزان ممكن **يبان متوازن** (لأنه بيجمع نفس الجزء المفقود من الطرفين المدين والدائن مع بعض) **مع إنه غير مكتمل فعليًا** — ده إحساس أمان زائف أخطر من رقم غلط واضح.

---

## ٣. تقرير الشركاء ← كشف شامل لشريك — انهيار كامل للمتصفح — ✅ مُصلَح

**أين:** `showPartnerStatement()` — `js/accounting.js:1682` وما بعدها.

**العطل:** الدالة بتجيب كل الملفات اللي الشريك مرتبط بيها (`partners_master`)، وبعدين **لكل ملف على حدة** بتطلق **9 طلبات API متوازية** (`purchase_orders`, `vehicles`, `expenses`, `sales`, `partners_master`, `payments`, `partner_payouts`, `collections`, `sale_charges`) + استدعاء `computePartnerSettlement()` + استدعاء `journal_entries` إضافي — **كل الملفات مع بعض دفعة واحدة عبر `Promise.all` بلا أي حد أقصى أو تقسيم دفعات (batching/throttling)**.

**الدليل الحي (2026-09-20):** تجربة "كشف شامل لشريك" لمازن الخلف من شاشة تقرير الشركاء رجّعت **أكتر من 500 خطأ `ERR_INSUFFICIENT_RESOURCES`** في الكونسول (المتصفح بيرفض يفتح اتصالات أكتر)، والتقرير فشل تمامًا وعرض "لا توجد بيانات"، مع رسالة خطأ:
> "خطأ في إعداد الكشف: ⚠️ انقطع الاتصال بالخادم أثناء العملية — قد تكون نجحت فعلاً رغم ظهور هذا الخطأ. تأكد من القائمة قبل إعادة المحاولة، حتى لا يتكرر البند"

**مشكلة إضافية في الرسالة نفسها:** الصياغة دي ("قد تكون نجحت فعلاً... حتى لا يتكرر البند") مكتوبة أصلًا لعمليات **كتابة/تعديل** بيانات (زي حفظ قيد)، مش لعملية **قراءة** تقرير. ظهورها هنا بيوهم المستخدم إن فيه بيانات ممكن تكون اتسجّلت مرتين أو فيه خطر تكرار — وهو مش الحال إطلاقًا في تقرير قراءة-فقط.

**الخطر (قبل الإصلاح):** أي شريك مرتبط بعدد ملفات كبير (مش بس مازن) كان بيقع في نفس المشكلة.

**✅ الإصلاح (2026-09-20، commit `bf805ce`):** نفس منطق كل ملف بالحرف (بلا أي تغيير في الحسابات)، لكن على دفعات من 5 ملفات كحد أقصى بدل كل الملفات دفعة واحدة. كمان اتصححت رسالة الخطأ عند فشل الاتصال (كانت تستخدم صياغة "قد تكون نجحت فعلاً... حتى لا يتكرر البند" المكتوبة لعمليات الكتابة، رغم إن التقرير قراءة فقط بلا أي خطر تكرار).

**تحقُّق حي بعد النشر:** تجربة "كشف شامل لشريك" لمازن الخلف (94 صفقة — أكبر عدد ملفات لأي شريك في النظام) نجحت بالكامل، طلعت الكشف الكامل ببياناته الحقيقية (تفصيل 94 ملف، حصص، أرباح/خسائر لكل ملف) بلا أي خطأ جديد في الكونسول (عدد أخطاء `ERR_INSUFFICIENT_RESOURCES` المتبقي من التجربة الفاشلة القديمة ثابت 497 قبل وبعد — صفر خطأ جديد أثناء التشغيل الناجح).

---

## ٤. مسار الوصول الصحيح موجود لكن غير واضح ولا موثَّق

**الشاشة الوحيدة اللي جربتها ورجّعت رقم مطابق 100% للحقيقة:** شجرة الحسابات ← الدوس على **اسم الحساب نفسه** (`showAccountLedger()` — `js/accounting.js:271-330`، شاشة "دفتر الأستاذ" لحساب واحد بس). دي مش معطوبة لأنها بتجيب حساب واحد فقط (`account_code=eq.X`)، فعدد صفوفها الفعلي (151 لحساب مازن) غالبًا تحت حد Supabase حتى لو إجمالي النظام كله فوقه.

**نتيجتها لحساب 2401 (2026-09-20):** 151 حركة، مدين 123,393.00، دائن 76,770.99، **الرصيد الختامي 46,622.01 مدين** — مطابقة تامة لنتيجة SQL المباشر.

**المشكلة:** مفيش أي رابط أو إشارة من "دفتر الأستاذ" في القائمة الرئيسية (البند ١، المعطوب) للمسار السليم ده. أي مستخدم عادي هيدخل من القائمة الرئيسية ويقع مباشرة على الشاشة المعطوبة — وده اللي حصل معايا فعليًا، واستغرق وقت طويل لاكتشاف إن فيه مسار تاني شغال.

---

## السبب الجذري المشترك

الإصلاح الصحيح (حلقة تكرار حقيقية تستخدم `Content-Range` من كل رد لمعرفة هل فيه صفحات زيادة، لا مجرد إرسال `Range` كبير مرة واحدة) **موجود بالفعل في الكود** — دالة `fetchOne` داخل `fetchJEForPeriod()`، `js/core.js:453` وما بعدها، ومُستخدَمة فعليًا لحساب بيانات لوحة التحكم (KPIs). هذا يعني إن إصلاح مشكلة "حد الـ1000 صف" المُكتشَفة سابقًا (مذكورة في الذاكرة تحت `project_1000row_pagination_bug`) **اتطبّق في استهلاك واحد بس (لوحة التحكم)، ولم يُعمَّم على باقي الأماكن** اللي بتجيب كميات كبيرة من `journal_entries` بنفس الطريقة القديمة المعيبة.

## ٥. دفتر الأستاذ العام — رصيد الجهة تحت الحساب يتجاهل الرصيد الافتتاحي — ✅ مُصلَح

**اكتُشف حيًّا 2026-09-20 (بعد إصلاح البنود ١-٤).** في شاشة دفتر الأستاذ العام، عند تحديد فترة مخصصة، رصيد الحساب نفسه (مثلًا 2401) بيُحسب صح (رصيد افتتاحي + حركة الفترة)، لكن عند التوسّع لمستوى الجهة تحته (مثلًا "مازن الخلف") كان الرصيد المعروض = حركة الفترة فقط بلا أي رصيد افتتاحي — رقم مختلف تمامًا عن رصيد الحساب الأب فوقه مباشرة، رغم إن كل حركات الحساب تخص نفس الجهة الوحيدة (كل حسابات الشركاء 24xx على هذا الشكل).

**السبب:** الرصيد الافتتاحي كان محسوبًا بمستوى الحساب فقط (`_computeOpeningBalances`)، بلا أي تفصيل بالجهة.

**✅ الإصلاح الجذري (commit `2573cb9`، Wrangler `2026-09-20T15:16:20Z`):** `_computeOpeningBalances` بقت ترجع `{ byAccount, byContact }` — `byAccount` زي القديم بالحرف (لا كسر لأي مستهلك)، و`byContact` تجميع إضافي بنفس مفتاح التجميع المستخدم لحركة الفترة. حُدثت المواضع الثلاثة المستهلكة، وأُضيف عرض سطر "رصيد افتتاحي" داخل الجدول التفصيلي لكل جهة (نفس شفافية مستوى الحساب بالحرف).

**تحقُّق حي:** TM 2401 (جهة واحدة، مازن الخلف) — قبل الإصلاح: 2,931.99 دائن (غلط)؛ بعده: 27,601.01 مدين، **مطابق تمامًا** لرصيد الحساب الأب. BOX 2100 (جهات متعددة، كناري) — مجموع أرصدة الجهات (7,107+80,000+72,000 دائن) = 159,107.00 دائن **مطابق تمامًا** لرصيد الحساب ككل، بلا أي انحراف.

---

## التوصية — تحديث الحالة (كل البنود مُقفَلة الآن)

1. ✅ **تم:** تطبيق نمط `fetchAllPages` على `loadNewLedger` و`_computeOpeningBalances` (commit `82ab4c8`) — منشور، مُتحقَّق حيًّا.
2. ✅ **تم:** نفس التطبيق على `loadTrialBalance` (بند ٢، commit `855c237`) — منشور، مُتحقَّق حيًّا (TM+BOX).
3. ✅ **تم:** إعادة بناء `showPartnerStatement` بدفعات من 5 ملفات كحد أقصى بدل `Promise.all` غير المحدود (بند ٣، commit `bf805ce`) — منشور، مُتحقَّق حيًّا (94 ملف لمازن، نجاح كامل).
4. ✅ **تم:** تصحيح رسالة الخطأ في مسار `showPartnerStatement` (نفس commit `bf805ce`).
5. ⏳ **متبقٍّ (منخفض الأولوية):** توضيح/تسهيل الوصول للمسار البديل (شجرة الحسابات ← اسم الحساب) لحساب واحد — أصبح أقل إلحاحًا الآن بعد إصلاح الشاشات العامة نفسها (بنود ١+٢).

كل بند لمس كود محاسبة/تقارير مشترك — نُفِّذ بموافقة صريحة من المالك لكل بند على حدة، مع `node --check`، حفظ CRLF، تحقق بايت-لبايت من النشر، وتحقق حي (SQL مباشر + شاشة) بعد كل نشر.
